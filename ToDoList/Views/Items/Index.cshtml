@{
  Layout = "_Layout";
}

@using ToDoList.Models;
@model List<ToDoList.Models.Item>;

<h1>Items</h1>

@if (@Model.Count == 0)
{
  <h3>No items have been added yet!</h3>
} 

@foreach (Item item in Model)
{
  <li>@Html.ActionLink($"{item.Description}", "Details", new { id = item.ItemId }) | @item.Category.Name</li>
}

<p>@Html.ActionLink("Add new item", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>


@* 
public string Name { get; set; }
public DateTime Date { get; set; }

public Item(string name)
{
  Name = name
}

public ActionLink(string one, string two)
{
  Name = one;
  Route = two;
}

public ActionLink(string one, string two, string three)
{
  Name = one;
  Route = two;
  Controller = three;
}

public ActionLink(string one, string two, Object obj)
{
  Name = one;
  Route = two;
  Id = obj;
}




Item item2 = new Item(name)
Item item1 = new Item(name,date) *@